// Reverses the order of elements in a list
// - A: The type of elements in the list
// - list: The input list to be reversed
// = A new list with the elements of the input list in reverse order

use List/{cons,nil}

reverse <A>
- list: (List A)
: (List A)

(List/reverse/go _ list (nil _))
