QBool
: *
= $(self: QBool)
  ∀(P: ∀(x: QBool) *)
  (Sigma
    U60
    λt
    ∀(x: 
      switch t = t {
        0: (P QBool.true)
        +: switch t = t-1 {
          0: (P QBool.false)
          +: ∀(x: Empty) * 
        }: * 
      }: *
    )
    (P self)
  )
