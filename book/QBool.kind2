QBool
: *
= $(self: QBool)
  ∀(P: ∀(x: QBool) *)
  (Sigma
    #U60
    λt
    ∀(x: 
      #match t = t {
        #0: (P QBool.true)
        #+: #match t = t-1 {
          #0: (P QBool.false)
          #+: ∀(x: Empty) * 
        }: * 
      }: *
    )
    (P self)
  )