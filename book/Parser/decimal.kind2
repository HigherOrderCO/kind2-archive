// Parses a string of decimal digits
// = A Parser that returns a String of consecutive decimal digits

use Char/{is_decimal}
use Parser/pick_while

decimal
: (Parser String)

(pick_while is_decimal)
