use Nat/{succ,zero,half,double}

bft (n: Nat) :
  (Equal Nat (half (double n)) n) =
  match n {
    succ:
      let ind = (bft n.pred)
      let prf = (Equal/apply _ _ _ _ succ ind)
      prf
    zero:
      {=}
  }
