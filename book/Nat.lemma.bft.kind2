use Nat.{succ,zero,half,double}

bft (n: Nat) : {(half (double n)) = n} =
  match n {
    succ:
      let ind = (bft n.pred)
      let prf = (Equal.apply _ _ succ _ _ ind)
      prf
    zero: {=}
  }
