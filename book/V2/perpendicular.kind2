// Finds a V2 vector perpendicular to the input V2 vector
// - a: The input V2 vector
// = A V2 vector perpendicular to `a`

use V2/{new}

perpendicular
- a: V2
: V2

match a {
  new:
    (new (- 0 a.y) a.x)
}